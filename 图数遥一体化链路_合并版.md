# 图数遥一体化无线链路 —— 原理、技术需求与实现方案

**合并版 v1.0　｜　编制日期：2026-09-10**

> 本文由 `图数遥一体化链路_原理与方案.md`（原理与实现主干）与 `方案.txt`（信息边界、验收口径、合规约束）合并而成，两份原始文档保留未改。
>
> 分析对象：Nebula-7 类无人系统数字通信链路（官方标称 1 中心 + 16 终端、星型组网、12 km、≤15 ms）。官方品牌 SINAVE，网站对应主体为广州燎疆科技创新有限公司。

---

## 零、阅读约定：本文的四类标注

**本文最重要的一条纪律：区分"厂商公开的"、"能算出来的"和"本文假设的"。** 全文所有指标按下表标注，不带标注的段落为工程分析与设计建议。

| 标注 | 含义 | 可以怎么用 |
|---|---|---|
| 【官方】 | 来自 SINAVE 官方产品页 / 手册目录 / 下载页的公开内容 | 可引用，但仍需厂商书面规格书确认测试条件 |
| 【推算】 | 由公开物理模型或标准算出，过程在文中给出，可独立复核 | 可用于量级判断和方案论证 |
| 【假设】 | **本文为讨论和设计而设定的数值，不是厂商指标，也不是实测结果** | 只能作为需求基线草案，**禁止直接写入 PRD 或对外材料** |
| 【待核实】 | 必须向厂商索取或实测才能确定 | 立项前置问题清单 |

> ⚠️ 特别提醒：原 `.md` 版本中的功耗、重量、防护等级、加密算法、入网时间、总物理层速率等数值，均属【假设】。本合并版已逐条标出。若此前有材料引用过这些数字，应回溯修正。

---

## 一、已核实的公开信息与信息边界

### 1.1 可以确认的公开信息【官方】

| 项目 | 官方公开内容 | 工程解读 |
|---|---|---|
| 业务类型 | 同一链路承载视频、状态数据和控制指令 | 产品定位不限于单向图传 |
| 网络结构 | 星型，最多 1 个中心节点连接 16 个终端 | **不能**据此推定支持 Mesh、多跳中继或中心节点自动接管 |
| 时延 | 两节点网络测试时延 ≤15 ms | 不能直接当作视频端到端时延，也不能直接外推至 16 节点满载 |
| 距离 | 最远 12 km，限定通视空对地条件 | 具体取决于型号、频段、功率、天线及环境 |
| 对外接口 | ETH ×2、UART ×2、SBUS ×1、USB 2.0 | 电平、针脚、方向、接口速率仍需完整规格书确认 |
| 配套软件 | 手册标注 V1.0；下载中心列 Windows 配置工具 V1.2 | **V1.2 是配置工具版本，不是设备固件版本** |

> 上述为厂家公开指标，非独立实测结果。

### 1.2 尚未核实的关键参数【待核实】

以下应作为选型前置问题向厂商索取，**不应自行补全**：

| 参数组 | 必须确认的内容 |
|---|---|
| 射频与空口 | 具体频段和型号、信道带宽、发射功率、接收灵敏度、调制编码、双工方式、是否跳频 |
| 数据承载 | 应用层有效吞吐量、上下行分配、不同距离下的速率、16 节点并发性能、串口封包机制 |
| 安全与管理 | 加密和认证方式、密钥管理、QoS、节点隔离、管理 API/SDK、升级与回滚机制 |
| 硬件与可靠性 | 供电范围、峰值电流、功耗、重量、尺寸、温度、防护等级、接口电气定义和认证范围 |
| 接口形态 | 官方仅列 "USB 2.0"；坊间资料出现的 "Type-C(NIC) + Type-C(UPGRADE) 双口" 说法未经证实 |

**不能仅凭"双天线""低时延""星型组网"，就断言该产品使用了 OFDM、MIMO、TDMA、AES-256 或某款芯片。** 这些是同类系统的**可行实现选项**，本文第二章据此展开原理分析 —— 那是"这类系统可以怎么做"，不是"它就是这么做的"。

---

## 二、工作原理（同类系统的可行实现路径）

> 本章为逆向推演与原理建模，用于理解技术方案。除标【官方】外，均为同类宽带数字链路的通用实现方式。

### 2.1 系统组成与角色划分

一对多的星型无线专网，两种角色：

| 角色 | 部署位置 | 职责 |
|---|---|---|
| 中心节点（Hub / Master） | 地面端，接手持地面站、笔电、遥控器 | 发射同步信号、统一分配时隙与带宽、维护网络成员表 |
| 终端节点（Remote / Slave） | 机载或车载，接飞控、云台 IPC、机器人控制器 | 同步入网、按分配时隙收发、对上层做协议透传 |

对上层设备而言，两端模块合起来相当于**一根"看不见的网线 + 几根看不见的串口线"**。飞控只知道自己在跟地面站说 MAVLink，并不知道中间隔着 12 km 无线。核心设计思想：**链路对业务透明**。

功能模型（基于官方接口的工程抽象，非拆机确认的内部结构）：

```text
移动端                                             地面端

网络摄像头/视频编码器 ─ ETH ┐                  ┌─ ETH ─ 视频解码与显示
飞控/机器人控制器    ─ UART ├─ 终端无线设备 ⇄ 中心无线设备 ─ 地面站
遥控接口            ─ SBUS ┘                  └─ 遥控输入/任务控制

                 视频与遥测主要向地面传输
                 控制指令主要向移动端传输
```

**视频部分：** 现有资料不足以证明设备内置视频编码器。方案应把视频压缩放在网络摄像头或伴随计算模块上，让链路传输**压缩后**的数据，不能假设可直接接入原始 HDMI 视频。这同时也是"低时延"和"低功耗"的关键设计决策。

### 2.2 "图数遥三合一"：一条链路承载三种业务

三类业务的物理性质完全不同，这是设计难点所在：

| 业务 | 方向 | 带宽 | 时延要求 | 可靠性要求 | 数据特征 |
|---|---|---|---|---|---|
| 图传（视频回传） | 下行（机→地）为主 | 2–20 Mbps | 中（100 ms 级可接受） | 允许丢帧 | 突发、大包、速率可变 |
| 数传（状态同步） | 双向 | 10–500 kbps | 中 | 需重传保证 | 周期小包 + 突发 |
| 遥控（控制指令） | 上行（地→机） | < 10 kbps | **极低且抖动要小** | **极高，不能丢** | 严格周期（如 SBUS 14 ms） |

**难点是调度，不是把数据简单混在一起。** 建议的处理原则：

| 业务 | 主要诉求 | 处理方式 |
|---|---|---|
| 实时控制 | 新鲜、及时、目的节点准确 | 最高优先级，限制排队时间，**过期指令直接丢弃** |
| 遥测与状态 | 连续、可判断链路质量 | 保证最低带宽；按重要程度设定更新频率 |
| 视频与文件 | 视频要吞吐，文件要完整 | 视频自适应降码率；文件传输置于最低优先级 |

对于连续的速度、姿态或摇杆指令，**旧指令即使最终成功送达，也可能已经没有执行价值** —— 这就是"过期即丢"而非"重传到底"的原因。

**复用方式：TDD 帧结构 + 时隙划分 + QoS 优先级调度。**

```
|<----------------- 一个 TDD 帧（如 5 ms） ----------------->|
| 同步/信标 | 遥控上行 | 数传双向 | 图传下行（剩余全部时隙）  | 保护间隔 |
|  固定     |  固定    |  半固定  |  动态分配                 |          |
```

- **遥控时隙固定预留**：无论图传多忙，遥控时隙永远在且位置固定 → 时延确定、抖动小。这是"控制指令必达"的根本保证。
- **图传占用剩余带宽，尽力而为**：链路变差时先降视频码率，而不是丢遥控。
- **数传插空并带 ARQ 重传**：小包、可重传，对时延不敏感。

三者在协议栈上表现为三个优先级队列，调度器按 `遥控 > 数传 > 图传` 严格优先级出队。

> 同目录 `nebula_mvp` 已在纯软件层面验证了这一机制：相同 500 kbps 受限容量下，FIFO 调度的遥控 P99 为 8223 ms，Fusion 优先级调度为 45.5 ms。见 `VERIFICATION.md`。该实验验证**算法机制**，不复现射频与实机性能。

### 2.3 物理层：数据是怎么飞出去的

典型发送链路：

```
业务数据 → CRC 校验 → 信道编码(LDPC/Turbo) → 交织 → 星座映射(QPSK/16QAM/64QAM)
        → OFDM 子载波映射 → IFFT → 加循环前缀CP → 上变频 → 功放 → 天线
```

接收端反向进行同步、信道估计、解调和纠错。

**关键技术点：**

- **OFDM 多载波调制**：把宽带信号拆成许多窄带子载波，每个子载波经历的信道近似平坦，配合循环前缀消除多径造成的符号间干扰。无人机低空飞行、地面反射多径严重，这是必选项。
- **自适应调制编码（AMC）**：接收端实时上报 RSSI / SNR / 误包率，发射端据此切换 MCS。近距离用 64QAM + 高码率跑满带宽；飞到 10 km 外自动降到 QPSK + 1/2 码率，牺牲速率保住连接。**这是"远距离"和"高码率"能兼得的原因 —— 它们不是同时成立的。**
- **TDD 双工**：收发同频、分时进行。上下行比例可按需配置（图传业务严重不对称，下行可配到 80% 以上），且不需要双工器，成本体积都低。
  - 注意区分：**TDD 是上下行分时使用无线资源；TDMA 是不同节点分时使用资源。** 两者在星型网中叠加使用。
- **双天线**：**双天线不自动等于两倍速率。** 天线可能服务于空间复用、接收分集、不同方向覆盖或收发配置。ADI 的 OFDM 参考设计就明确区分单天线模式和空间分集模式。近距离可做空间复用提升速率，远距离退化为接收分集提升灵敏度。
- **抗干扰**：跳频、自动选频（上电扫描全频段选最干净信道）、窄带干扰陷波。

**可复用的公开技术基础：**

- **ADI AN-2597** —— 基于 AD936x 的 OFDM HDL 参考调制解调器，FPGA 基带 + 射频收发器组合，支持多种调制编码及空间分集模式。适合作为收发链和基带验证起点，**但它不是可直接替代整套工业数据链的成品方案**。
- **openwifi** —— 与 Linux mac80211 兼容的 SDR Wi-Fi 实现，公开低层 MAC 基于 DCF/CSMA/CA，提供按 MAC 地址进行时间切片的能力。**不能把这些功能等同于已完成面向本项目的确定性 1 对 16 调度。** 项目同时存在开源和商业授权安排，复用前需检查各组成模块许可证。

### 2.4 链路预算：12 km 是怎么来的【推算】

> 以下为按 ITU-R P.525 自由空间模型的计算值，**不是对该产品工作频段的认定，也不是实测损耗**。

自由空间路径损耗：

```
FSPL(dB) = 32.44 + 20·lg(f/MHz) + 20·lg(d/km)
```

以 2.4 GHz、12 km 计算：`32.44 + 67.60 + 21.58 ≈ 121.6 dB`
（同条件 5.8 GHz 约 129.3 dB）

链路预算：

```
接收功率 = 发射功率 + 发射天线增益 + 接收天线增益 − 路径损耗 − 馈线/极化损耗
         = 30 dBm(1W) + 5 dBi + 8 dBi − 121.6 dB − 3 dB
         ≈ −81.6 dBm
```

接收灵敏度（5 MHz 带宽、QPSK 1/2）：

```
热噪声底 = −174 + 10·lg(5×10⁶) = −107 dBm
+ 噪声系数 5 dB → −102 dBm
+ 解调所需 SNR 3 dB → 灵敏度 ≈ −99 dBm
```

**链路余量 ≈ 17 dB** —— 数字上完全成立。这解释了几件事：

1. **12 km 是窄带低阶调制下的距离**，此时视频码率必然很低。宣传页把"12 km"和高码率图传分开写，不是巧合。
2. **频率越低传得越远**。1.4 GHz 比 2.4 GHz 路损少约 4.7 dB，5.8 GHz 则多约 7.7 dB。行业设备偏好 1.4 GHz 正是为此（但见 §3.3 的合规约束 —— 1.4 GHz 在中国境内不能这么用）。

**还必须满足视距和菲涅尔区净空**（"开阔无遮挡"的量化含义）：

```
第一菲涅尔区半径 r₁ = 8.66·√(d/f) = 8.66·√(12/2.4) ≈ 19.4 m   (d: km, f: GHz)
地球曲率隆起（中点，k=4/3） ≈ 2.1 m
→ 收发连线中点需离开障碍物约 21.5 m 以上
```

结论：**地面对地面 12 km 需要架高塔；无人机飞到 100–300 m 高度天然满足。** 这就是为什么这类链路的标称距离总在空对地场景才能复现。

**工程上必须分别完成两个方向的链路预算**，再把接收功率与**目标带宽、目标 MCS 下的接收灵敏度**比较，预留衰落余量。**不能拿最低速率下的最远距离，去承诺最高速率下的业务表现。**

> 最终要验证的是同一个工作点：**在指定距离、天线安装、节点数量和视频负载下，同时满足吞吐量、控制时延和可靠性** —— 而不是分别测出几个最好值再拼在一起。

### 2.5 时延分解：≤15 ms 到底是哪一段

官方脚注已承认："不代表摄像头采集至显示端的完整视频链路时延"。

完整端到端时延：

```
T(视频端到端) = T(采集) + T(编码) + T(排队) + T(无线传输) + T(解码) + T(显示)
```

| 环节 | 典型耗时【假设/经验值】 |
|---|---|
| 摄像头曝光 + 出图 | 1 帧 ≈ 33 ms @30fps（可用高帧率降低） |
| H.265 编码 | 5–30 ms |
| 打包 + 发送缓冲 | 2–10 ms |
| **空口传输（TDD 帧对齐 + 调度 + 空中飞行）** | **5–15 ms ← 厂商标称的就是这一段【官方】** |
| 接收抖动缓冲（jitter buffer） | 10–50 ms |
| 解码 + 渲染显示 | 10–30 ms |
| **合计（真实体感）** | **80–160 ms** |

**理解要点：15 ms 是"模块入口到模块出口"的链路时延，是模组厂商唯一能负责的部分**；真正影响操控手感的大头在编解码和抖动缓冲。做系统集成时，优化重点往往不在链路而在编码器配置和 jitter buffer 策略。

**遥控通道要单独看**：它不经过编解码，`地面 SBUS 输入 → 空口 → 机载 SBUS 输出` 的时延就是链路时延本身，通常 < 20 ms，这才是"低时延"真正有意义的地方。

**两个测量口径问题【待核实】：**

1. 官方的"两节点网络测试时延"未说明测量起止点、包长、负载、统计方式，以及是单向还是往返。它只能作为**待复测指标**。
2. **必须区分单向时延与往返时延**：上下行排队和资源分配可能不同，**不能简单把 ping 的往返值除以二当作单向结果**。RFC 7679 单向时延规范强调了路径、方向和时钟误差的影响。

**量级参照：** 按自由空间传播速度，12 km 单向传播时间约 **0.040 ms**。因此毫秒级延迟主要应从处理、封包、调度、排队和重传环节排查，**而不是归因于电波"走得太远"**。

### 2.6 星形自组网与调度

**入网流程（终端上电后自动完成）：**

```
扫频搜索信标 → 频率/时间同步 → 发起入网请求（携带 ID/能力）
→ 中心节点鉴权、分配短地址与时隙 → 进入业务态 → 周期心跳维持
```

**为什么是星形而不是 Mesh：**

| | 星形（本方案） | Mesh |
|---|---|---|
| 调度 | 中心统一分配，时隙确定 | 分布式竞争/路由 |
| 时延 | 确定、可预测 | 随跳数和路由收敛波动 |
| 覆盖 | 受限于中心节点覆盖范围 | 可多跳延伸 |
| 可靠性 | **中心失效则全网瘫痪** | 无单点故障 |
| 适用 | 集群作业、编队，要求控制时延确定 | 大范围、无固定基础设施 |

对"控制类"业务，**时延确定性远比覆盖延展性重要**，所以选星形。代价是单点故障 —— 中心节点故障后的行为是【待核实】的关键问题之一。

**16 个终端的带宽约束（重要）：**

"1 带 16" 是**拓扑容量，不是并发满码率能力**。两种独立算法得到一致结论：

*算法一（自顶向下）*【假设】：若总物理层速率约 40 Mbps，16 节点平均每台仅约 2.5 Mbps —— 够 720p H.265，不够 1080p 高码率。

*算法二（自底向上）*【推算】：设每终端视频 4 Mbps、控制与遥测 0.2 Mbps，并把平均负载控制在**已测得稳定容量的 70%** 以内：

```
所需有效容量 ≥ N × (4 + 0.2) / 0.7
```

- **4 个终端：约需 24 Mbps 稳定应用有效容量**
- **16 个终端：约需 96 Mbps 稳定应用有效容量**

> 70% 是建议的容量余量策略，不是协议效率。测量"稳定应用有效容量"时已应包含无线调度、重传的影响，不要重复或遗漏计算。

**两条路径都指向同一结论：16 路高清视频并发不成立。** 实际使用必然是**按需动态分配** —— 全部终端保持控制和遥测，**只有被"点名"的少数终端回传主码流**，其余使用低码率预览或按需切换。**这是方案设计时必须写进需求的约束，不是可选优化。**

### 2.7 接口层：怎么接进现有系统

模块本质是一个**跨越无线的透明网桥 + 多路串口隧道**。

> 下表接口**清单**为【官方】；协议映射与实现方式为【假设】，电平、针脚、方向、速率均为【待核实】。

| 接口 | 对接设备 | 承载协议 | 推测实现方式 |
|---|---|---|---|
| ETH1 / ETH2【官方】 | 云台 IPC、机器人控制器、地面站 PC | RTSP/RTP 视频流、ROS2/DDS、TCP/UDP | 二层网桥，两端同网段，MAC 地址透传【假设】 |
| UART1 / UART2【官方】 | 飞控、载荷 | MAVLink、Modbus、自定义 | 串口数据封装成帧，透明隧道【假设】 |
| SBUS【官方】 | 遥控接收机 / 舵面 | SBUS（100 kbps、8E2、反相、25 字节、周期 14 ms） | 解析 16 通道值后单独走高优先级时隙【假设】 |
| USB 2.0【官方】 | 地面端笔电 / 上位机 | 虚拟网卡 / 固件升级 / 参数配置 | 具体是单口复用还是双 Type-C 口，**未经证实**【待核实】 |

**SBUS 的处理方式值得单独说明**：合理设计不是简单透传字节流，而是**解析出通道值再重新组包**。这样 14 ms 一帧的数据只需几十字节，可以塞进最小的固定时隙，且**丢一帧可以用上一帧的值填充，操控不会跳变**。

**接线前必须取得当前型号的接口定义**，尤其确认 UART 电平、SBUS 方向及反相要求、供电针脚、USB 用途。**不要从接口外形推断供电能力，也不要把历史文档图片当作当前针脚定义。**

**上层协议建议：** 遥测与控制建议采用有明确设备地址、消息类型和序号的应用协议。无人机项目可评估 MAVLink（系统 ID / 组件 ID 区分载体与设备，消息序号可监测丢包）。**但这属于上层协议选择，不意味着该模块原生解析 MAVLink。**

---

## 三、技术需求（带验收口径）

> **本章所有数值为 V0.1 需求基线草案，不是实测能力，也不是厂家承诺。** 每条需求都必须有验收口径，否则无法判定达成。

### 3.1 功能需求

| 编号 | 需求 | 标注 | 验收口径 |
|---|---|---|---|
| F-01 | 单链路并发承载图传/数传/遥控三类业务，遥控最高优先级 | 【官方】业务类型 /【假设】优先级机制 | 视频满载注入下，遥控时延分布不发生显著漂移 |
| F-02 | 星形组网，1 中心 + ≥16 终端，上电自动入网 | 【官方】拓扑 | 节点加入、掉线、重连、目的节点隔离逐项测试 |
| F-03 | 以太网二层透明桥接 | 【假设】 | 两端设备同网段直接互访；测重复 IP / 广播风暴行为 |
| F-04 | 双路 UART 透传，波特率 9600–921600 可配 | 【待核实】速率范围 | 各档位下误码、丢包、封包边界完整性 |
| F-05 | SBUS 16 通道解析与还原，输出周期稳定 | 【假设】实现方式 | 示波器测输出周期抖动；丢帧时的填充行为 |
| F-06 | 自动选频与抗干扰，运行中可切换 | 【待核实】 | 信号源注入同频/邻频干扰，记录降级与恢复 |
| F-07 | 带宽动态分配，支持指定主视频源 | 【假设】 | 切换主视频源时，其余节点遥控/遥测不中断 |
| F-08 | 链路状态可视化：RSSI、SNR、丢包率、当前 MCS、实时码率 | 【待核实】是否提供 API/SDK | 状态可通过管理接口读取并记录，非仅界面显示 |
| F-09 | 断链自动重连，恢复时间 < 3 s | 【假设】 | 人为断电/遮挡 100 次，统计恢复时间**分布**而非平均值 |
| F-10 | 加密与鉴权：空口加密 + 白名单入网 | 【待核实】**具体算法未经证实** | 非授权节点不得取得控制权；验证防重放 |
| F-11 | 固件在线升级，中心节点可远程升级终端 | 【待核实】含回滚机制 | 升级中断后可恢复；异常配置可回退 |

### 3.2 性能需求

> ⚠️ 除标【官方】外，本表数值全部是**本文设定的讨论基线**。

| 指标 | 目标值 | 标注 | 验收口径 |
|---|---|---|---|
| 视距通信距离 | ≥ 12 km | 【官方】标称 | 空对地、机载高度 ≥150 m、无遮挡、最低阶 MCS；**须同时记录该距离下的实际速率** |
| 空口单向时延 | ≤ 15 ms | 【官方】标称 | **须索取测量口径**：起止点、包长、负载、单向/往返、统计方式 |
| 遥控端到端时延 | ≤ 25 ms | 【假设】 | SBUS 输入到 SBUS 输出，示波器实测（见 §6.2） |
| 遥控时延抖动 | ≤ ±3 ms | 【假设】 | 连续 10000 帧到达间隔，统计标准差与 P99 |
| 控制时延（网络业务） | 单向 P95 ≤20 ms、P99 ≤40 ms | 【假设】 | 固定节点数、负载、信道条件；记录测量误差与时钟同步偏差 |
| 控制可靠性 | 100 ms 截止时间内有效交付率 ≥99.9% | 【假设】 | **超时包按业务失败计**；另统计连续丢失时长 |
| 链路总吞吐 | ≥20 Mbps @2 km；≥2 Mbps @12 km | 【假设】 | 测**应用有效载荷**，不用网口速率或物理层标称值代替 |
| 4 路视频场景有效吞吐 | ≥ 25 Mbps | 【假设】 | 同上；对应 §2.6 算法二 |
| 单终端保底带宽 | ≥ 512 kbps | 【假设】 | 16 节点满载时**逐台**验证，非平均值 |
| 视频时延 | 采集到显示 P95 ≤150 ms | 【假设】 | **完整测量编码、网络、解码、显示**，不能用网络包时延代替 |
| 遥控丢包率 | ≤ 10⁻⁵（含重传） | 【假设】 | 长时间连续采样，非短窗口 |
| 入网时间 | ≤ 5 s（冷启动） | 【假设】 | 100 次冷启动统计分布 |
| 频段 | 依当地法规确定 | 【待核实】 | **见 §3.3，1.4 GHz 有硬性使用限制** |
| 发射功率 | ≤ 30 dBm 可调 | 【假设】 | 须满足所在区域 EIRP 法规限值 |
| 功耗 | ≤ 8 W（终端） | 【假设】**无来源** | 典型业务 + 满载 + 启动峰值电流分别测 |
| 供电 | 9–36 V DC 宽压 | 【待核实】 | 低电压边界、上电冲击、纹波容限 |
| 重量 | 终端 ≤ 150 g | 【假设】**无来源** | 含天线与线缆的实称重量 |
| 工作温度 | −20 ℃ ~ +60 ℃ | 【假设】**无来源** | 无风冷自然散热，测满载温升与是否降频 |
| 防护等级 | IP54 及以上 | 【假设】**无来源** | 须厂商认证报告 |

> P95、P99 分别表示 95%、99% 的样本不超过相应值。上述目标需结合具体载体收紧或放宽，**不能自动视为安全认证指标**。

### 3.3 合规约束（硬性，优先于一切性能设计）

**中国境内民用无人驾驶航空器**的直连通信频率适用工信部《民用无人驾驶航空器无线电管理暂行办法》：

- **1430–1444 MHz 仅用于遥测与信息传输下行链路**，且使用涉及频率许可和电台执照。
  **⚠️ 不能因为配置界面出现"1.4G"选项，就把它当作可自由双向使用的频段。** §2.4 中"1.4 GHz 路损更小"的物理结论成立，但**不构成可用性结论**。
- **2400–2476 MHz、5725–5829 MHz** 有相应设备技术要求，需符合功率和占用带宽规定。

**因此：应先确认应用类别、地区、频段和具体型号的适用条件，再设计射频前端和空口参数。**

**机器人项目不应直接套用无人机的频率使用规定** —— 适用规则不同，须单独确认。

方案必须支持**按区域配置频率表和功率上限**。

### 3.4 安全与失联保护（必须与"连得上"分开设计）

**安全要求分四层独立设计**，不要合并成一个"加密开关"：

1. **节点身份认证** —— 非授权节点不得入网、不得取得控制权
2. **消息完整性 / 防重放** —— 时间戳 + 序列号
3. **机密性** —— 需要保密时另行配置加密承载
4. **可追踪性** —— 配置和日志可审计，异常配置可恢复

> 采用 MAVLink 时注意：**消息签名用于验证可信来源，并不提供消息加密。**

**失联保护不宜只有一个总开关。** 以 PX4 为例，其官方文档**区分遥控丢失和地面站数据链丢失，分别配置超时与动作**。需求分解应覆盖：遥控丢失、数据链丢失、机载计算机故障等不同情形，具体设置必须对应所用控制器版本和载体。

链路中断后，机载端 SBUS 输出需按预设策略动作（保持 / 返航信号 / failsafe 值），**不能输出乱码**。超时门限及安全动作按载体风险分析确定。

**无线中断时，本机仍应能执行预定安全策略 —— 实时内环控制必须留在本机。**

---

## 四、路线 A：成品集成先行验证（建议优先推进）

> **这是启动自研前必须先走完的一步。** 目的不是证明所有宣传指标，而是尽快回答：成品能否满足实际业务，哪些能力必须由外部系统补充。

### 4.1 系统组成

- **移动端**：网络摄像头 / 编码模块 + 飞控或机器人控制器 + 接口适配板 + 终端模块
- **地面端**：中心节点模块 + 地面站计算机 + 操控设备 + 视频显示与记录软件

**首轮只用一对无线设备和一套业务设备。** 确认两节点基线后再增加终端，避免同时引入无线、视频、供电和多机寻址问题。

### 4.2 网络与协议设计

- 采用**固定的设备地址规划**，明确每个终端与摄像头、控制器的对应关系。多机接入时检查重复 IP、重复设备 ID 和广播控制风险。
- 视频试验采用压缩视频经 **RTP/UDP** 传输。注意：**RTP 提供序号和时间戳等机制，但本身不保证带宽、及时交付或服务质量。**
- 控制和遥测优先沿用载体已有协议。需要外部网关时，网关负责**目的节点检查、时间戳、命令有效期、流量限制和日志**，而不是无条件转发所有收到的数据。

### 4.3 低时延与拥塞控制

编码端先采用低时延配置并测量画质代价。例如 GStreamer `x264enc` 提供 `tune=zerolatency`，但官方文档明确提示**它可能影响编码质量，且不是整条视频链路"零延迟"的保证**。

在每个视频源处设定码率上限，在地面端记录**控制时延随视频负载变化的曲线**。链路恶化时依次：降低视频码率 → 减少并发视频 → 暂停文件传输。

> ### ⚠️ 集成路线最大的坑
>
> **外部网关的优先级标记和限速，不等于无线模块内部一定执行同样的优先级。**
>
> 你在网关上把控制包标成最高优先级，模块内部完全可能仍然按 FIFO 把它排在几十个视频包后面 —— 从外部看不出来，只有做拥塞测试才会暴露。
>
> **必须通过拥塞测试确认控制包是否仍被保障。** 若厂商不提供内部 QoS 能力或管理接口，就应采用更保守的业务负载，或将此列为切换路线的触发条件。

### 4.4 决策门槛

**继续集成的条件：** 实测满足容量、控制时延、接口兼容和失联要求 → 进入载体集成与环境验证。

**转向自研的条件（任一成立即评估）：**

- 内部调度不可控（§4.3 的拥塞测试不通过，且厂商无 QoS 接口）
- 缺少必须的管理接口 / SDK
- 频段或形态不匹配业务需求
- 多节点容量确认无法支撑业务规模

**不构成自研理由：** 仅仅是视频码率过高 —— 先调整业务配置。

> **立项评审的第一道门槛应是：完整规格书 + 双节点测试报告 + 多节点拥塞测试结果。**
> 只有这三项支持实际业务需求，才值得进一步冻结天线、供电、结构和整机方案。
> **自研应由明确的能力缺口驱动，而不是仅凭"12 km、15 ms、16 节点"三个独立数字启动。**

---

## 五、路线 B：自研同类链路实现方案

> 目标应定义为**实现相近的业务能力**，而不是默认与 Nebula-7 无线互通。后者需要明确的空口协议资料或厂商合作，仅有外部接口说明通常不够。

### 5.1 技术路线选型

| 路线 | 描述 | 优点 | 缺点 | 评价 |
|---|---|---|---|---|
| A. 私有图传 SoC | 海思/展锐等专用图传基带芯片 | 集成度高、功耗低、量产成本低 | 协议栈黑盒，定制自由度低，供应受限 | 适合量产，不适合学习/研究 |
| **B. SDR 自研（原型推荐）** | AD9361/AD9364 + Zynq/ZU+ FPGA 基带 + ARM 协议栈 | 物理层完全可控，可自定义 TDMA 帧结构，便于研究与迭代 | 开发量大，功耗体积偏高，成本高 | **原理理解与原型验证首选** |
| C. LTE/5G 私网模组 | 私网基站 + CPE（如 1.4 GHz 宽带专网） | 协议成熟，多用户调度现成 | 时延偏大（20–50 ms），成本高，架构重 | 适合大范围指挥，不适合低时延遥控 |
| D. Wi-Fi 改造 | 802.11 芯片改 TDMA 固件 | 成本极低 | CSMA 本质不适合确定性调度，改造深度受限 | 仅适合低成本入门 |

**建议：原型阶段走 B（SDR），验证帧结构、调度算法和 AMC 策略；产品化阶段再评估是否切到 A 降本。**

> 前提：**已完成 §4.4 的决策门槛判断。** 在成品尚未验证前直接启动自研，是把最贵的路径放在最前面。

### 5.2 硬件架构框图

```
┌──────────────────────────────────────────────────────────┐
│                      终端 / 中心节点模块                   │
│                                                          │
│  [天线1]──┐                                              │
│           ├─[射频前端: PA/LNA/收发开关/滤波器]            │
│  [天线2]──┘            │                                 │
│                        ▼                                 │
│              [AD9361 宽带收发器 2T2R]                     │
│                        │ LVDS                            │
│                        ▼                                 │
│   ┌────────────────────────────────────────┐             │
│   │  Zynq UltraScale+ MPSoC                │             │
│   │  ┌──────────────┐  ┌────────────────┐  │             │
│   │  │ PL (FPGA)    │  │ PS (ARM A53)   │  │             │
│   │  │ OFDM 调制解调 │◄─┤ MAC 调度器      │  │             │
│   │  │ LDPC 编解码  │  │ 组网/入网管理    │  │             │
│   │  │ 同步/信道估计 │  │ QoS 队列        │  │             │
│   │  │ AGC/定时环   │  │ 网桥/串口隧道    │  │             │
│   │  └──────────────┘  └────────────────┘  │             │
│   └────────────────────────────────────────┘             │
│         │         │         │        │                   │
│      [GMAC]    [UART×2]  [SBUS]   [USB]                  │
│         │         │         │        │                   │
│   [ETH1/ETH2]  [飞控]   [接收机]  [Type-C]                │
│                                                          │
│  [DC-DC 9~36V] [温度监测] [铝合金散热外壳]                │
└──────────────────────────────────────────────────────────┘
```

要点：

- **视频不在模块内编码** —— IPC 已输出 H.265 码流，模块只做透传。这是"低时延"和"低功耗"的关键设计决策。
- **外壳即散热器**。FPGA 方案功耗高，热设计是硬约束。
- AD9361 的双发双收、TDD/FDD、可调通道带宽是**射频器件能力**，**不等于已经实现了完整通信协议、视频传输或确定性调度**。

### 5.3 软件分层

```
┌─────────────────────────────────────────┐
│ 应用层  配置管理 / 状态上报 / OTA 升级     │
├─────────────────────────────────────────┤
│ 适配层  以太网桥 | 串口隧道 | SBUS 编解码  │
├─────────────────────────────────────────┤
│ 网络层  节点管理 / 路由(星形直连) / 加密   │
├─────────────────────────────────────────┤
│ MAC 层  TDMA 帧调度 | QoS 队列 | ARQ 重传 │
│         带宽动态分配 | AMC 决策           │
├─────────────────────────────────────────┤
│ 物理层  OFDM 收发 / 编译码 / 同步 (FPGA)  │
└─────────────────────────────────────────┘
```

### 5.4 关键算法设计

**MAC 层四条设计原则：**

1. **控制保底** —— 为控制和关键遥测预留资源，不能让视频占满全部发送机会
2. **视频按需分配** —— 按终端需求和链路质量分配剩余容量；**终端信号变差时同步降低其视频负载，避免低速节点长期占用大量空口时间**
3. **按业务决定重传** —— 参数设置、任务下发需确认；连续实时指令要控制重试次数与截止时间，**过期后不再执行**
4. **在硬件中处理精确定时** —— 收发切换、时隙边界和关键队列由 FPGA 或专用实时逻辑实现；操作系统只负责配置和非实时管理

**① TDMA 帧结构（示例，5 ms 帧）**【假设】

| 时隙 | 长度 | 用途 |
|---|---|---|
| BCH | 0.2 ms | 中心节点广播信标：帧号、时隙分配表、系统参数 |
| UL-CTRL | 0.3 ms | 遥控上行，固定预留 |
| UL-DATA | 0.5 ms | 终端数传上行 + 入网请求随机接入 |
| DL-DATA | 0.5 ms | 数传下行 + 调度指令 |
| DL-VIDEO | 3.3 ms | 图传下行，动态分配给各终端 |
| GP | 0.2 ms | 保护间隔 |

> **保护间隔必须单独核算**：12 km 传播时延约 40 μs，另加时钟误差和射频收发切换时间。**不能直接套用近距离测试结果** —— 远距离、多节点场景下这是常见的翻车点。

**② AMC 决策**：滑动窗统计 SNR 与 BLER，**双门限迟滞**切换 MCS，防止边界抖动。

```
if BLER > 10% for 3 frames:                                  降一级 MCS
if SNR > threshold[k+1] + 2dB and BLER < 1% for 20 frames:   升一级 MCS
```

**③ 带宽动态分配**：中心节点每 100 ms 重算一次时隙表。保底带宽先分配给所有在网终端，剩余带宽按"当前主视频源 > 有数据待发 > 空闲"加权分配。

**④ 失控保护**：终端连续 5 帧未收到信标即判定失联，SBUS 输出切换到预设 failsafe 值并置位失效标志给飞控。

> 以上均为建议架构，**必须通过仿真和原型测试验证**。

### 5.5 开发阶段规划【假设，非交付承诺】

| 阶段 | 内容 | 里程碑 |
|---|---|---|
| P1 链路预算与选型（2 周） | 频段确定、器件选型、链路预算表、**法规调研** | 方案论证报告 |
| P2 物理层仿真（4 周） | MATLAB/GNU Radio 搭 OFDM 收发链，加多径信道验证 BER 曲线 | 仿真达标 |
| P3 单点对传（6 周） | FPGA 实现物理层，实验室有线对接跑通单向数据 | 桌面短距通信成功 |
| P4 MAC 与组网（6 周） | TDMA 调度、入网流程、QoS 队列、AMC | 1 中心 + 3 终端稳定组网 |
| P5 接口适配（3 周） | 网桥、串口隧道、SBUS，接真实飞控与 IPC | 联调通过 |
| P6 外场测试（4 周） | 距离、时延、抗干扰、多机并发实测 | 指标达标报告 |

**周期说明：** 上表合计约 25 周。对具备射频、FPGA 和嵌入式经验的团队，**功能原型 3–6 个月是乐观估计**；**工程化、认证和量产验证另计**，且需单独承担射频匹配、功放线性度、滤波、供电噪声、散热、收发隔离、驱动、异常恢复和量产测试等工作。

**推进顺序建议：优先证明"业务可用"，再做小型化与低功耗，最后做环境和合规验证** —— 比一开始同时追求全部指标更可控。

---

## 六、验证计划与测试方法

### 6.1 四阶段推进

| 阶段 | 核心工作 | 应交付的结果 |
|---|---|---|
| 规格澄清 | 获取完整手册、型号表、接口定义、测试条件、软件版本对应关系 | 需求基线、接口控制文件、**参数缺口清单** |
| 双节点基线 | 同时测视频、控制、遥测；测单向时延、有效吞吐、丢包、温升 | 可复现配置、原始日志、性能边界 |
| 多节点压力测试 | 逐步扩展到 4、8、16 终端；加入视频突发、弱链路节点、掉线重连 | 容量曲线、公平性、控制时延与拥塞表现 |
| 场景与故障验证 | 按许可条件验证距离、遮挡、姿态、机身干扰、断电与失联保护 | 使用边界、故障处置结果、是否进入工程化的结论 |

**集成路线（路线 A）首轮验证：在样机到位、接口资料完整、已有业务软件的假设下，可暂按 4–8 周安排。**

### 6.2 逐项测试方法

| 指标 | 方法 |
|---|---|
| 通信距离 | 外场空对地飞行，逐点记录 RSSI/BLER/吞吐**随距离曲线**；先在实验室用**可变衰减器模拟等效路损**做标定 |
| 空口时延 | **两端接同一台示波器**，在入口和出口打触发脉冲直接量时间差。**不要用软件打时间戳 —— 会引入系统误差** |
| 单向时延（网络业务） | 两端时间戳法须**校准时钟同步与主机处理误差**；记录包长、发送频率、负载、方向和测量点 |
| 视频端到端时延 | **另做完整的采集到显示测试**（如时钟屏拍摄法），**不能用网络包时延代替** |
| 时延抖动 | 连续采集 10000 个遥控帧的到达间隔，统计标准差与 P99 |
| 吞吐 | iperf3 跑 UDP 逐级加压找拐点；**测应用有效载荷，不用网口速率代替** |
| 多机并发 | 16 台终端同时入网，观察各自保底带宽是否满足、入网是否互相干扰 |
| 拥塞下的 QoS | **视频满载注入，观察控制包时延分布是否漂移** —— 验证 §4.3 的核心风险 |
| 抗干扰 | 信号源注入同频/邻频干扰，记录链路降级和恢复行为 |
| 失联保护 | 断电/遮挡/干扰三种断链方式各测；确认 SBUS 输出行为符合预设 |

### 6.3 应优先向厂商索取的资料

最有决策价值的不是更多宣传材料，而是以下书面证据：

1. **完整规格和接口文件** —— 型号与频段、供电、针脚、电平、SBUS 方向、网口能力、配套软件版本
2. **带测试条件的性能报告** —— 尤其是 ≤15 ms 的**测量口径**，以及 12 km 对应的速率、视频负载、天线、安装高度和丢包情况
3. **多节点和业务调度说明** —— 16 终端总有效容量、上下行分配、**是否支持优先级**、弱链路节点对其他节点的影响、**中心节点故障后的行为**
4. **安全、管理和合规资料** —— 认证与加密机制、SDK/API、状态读取、升级回滚，以及具体型号适用的核准和使用条件

官网技术联系邮箱：Support@sinave.com

---

## 七、风险清单

| 风险 | 影响 | 应对 |
|---|---|---|
| **外部 QoS ≠ 模块内部 QoS** | 网关标了优先级但模块内部仍 FIFO，控制包排在视频后面 —— **外部观测不到，只有拥塞测试会暴露** | 拥塞测试列为强制项；厂商无内部 QoS 接口时采用保守负载或触发路线切换 |
| 12 km 与高吞吐不可兼得 | 需求冲突、对外承诺失实 | 需求中**分档定义**：距离指标与吞吐指标分开，各自写清测试条件 |
| 16 节点带宽不足 | 用户体感与宣传不符 | 明确"1 主视频 + N 路数传遥控"的典型工作模式，写进需求 |
| **频段合规（1.4 GHz）** | 无法合法使用，或只能单向 | 提前确认频段许可；软件支持区域参数表；机器人项目单独确认适用规则 |
| 中心节点单点故障 | 全网瘫痪 | 【待核实】厂商是否有接管机制；自研需设计降级模式 |
| FPGA 功耗与散热 | 高温降频、机载重量超标 | 早期做热仿真；评估低功耗基带替代方案 |
| 多径与地面反射 | 低空段链路劣化 | 加强信道估计与均衡，采用天线分集 |
| 远距离保护间隔不足 | 多节点时隙碰撞 | 保护间隔按传播时延 + 时钟误差 + 收发切换单独核算，不套用近距结果 |
| **【假设】数值被误当作规格** | 对外材料失实、需求基线错误 | **§0 标注体系强制执行**；对外发布前逐条核对标注 |

---

## 八、总结

**技术上的一句话：**

> 这套系统的本质是：**用 TDD 帧结构和严格优先级调度，把三类性质迥异的业务塞进同一条 OFDM 无线链路** —— 用固定时隙保住遥控的时延确定性，用自适应调制编码换取距离与速率的动态平衡，用中心节点集中调度实现 16 台设备的确定性组网，再用透明网桥和串口隧道让上层设备感知不到无线的存在。

**决策上的一句话：**

> **先把 Nebula-7 当作待验证的通信模块，而不是已经满足系统要求的完整通信方案。** 最优先的工作是用一对样机建立可复现基线，再用 1 对 4 的视频与控制混合负载确认容量、尾部时延和失联行为。自研应由明确的能力缺口驱动。

**方法上的一句话：**

> 本文第二章讲的是"这类系统可以怎么做"，不是"它就是这么做的"；第三章的数值是讨论基线，不是规格书。**把这两句话删掉，整份文档的可信度就归零了。**

---

## 附录：引用来源

| 编号 | 来源 | 用于 |
|---|---|---|
| [1] | SINAVE 官方产品页 — NEBULA-7 数字通信链路系统 · `sinavelyra.com/products/nebula-7` | §1.1 全部官方指标 |
| [2] | SINAVE 技术手册页 · `sinavelyra.com/support/manuals/nebula-7` | 手册版本 V1.0、目录结构 |
| [3] | SINAVE 下载中心 · `sinavelyra.com/support/downloads` | 配置工具 V1.2、技术联系邮箱 |
| [4] | MAVLink FAQ · `mavlink.io/en/about/faq.html` | §2.7 系统/组件 ID；§3.4 签名不提供加密 |
| [5] | ADI AN-2597 — OFDM-Based HDL Reference Modem Using AD936x | §2.3 可复用基础；空间分集 vs 单天线 |
| [6] | ADI AD9361 产品页 | §5.2 射频器件能力边界 |
| [7] | RFC 7679 — One-way Delay Metric for IPPM | §2.5 单向/往返区分；§6.2 时钟误差 |
| [8] | RFC 3550 — RTP | §4.2 RTP 不保证 QoS |
| [9] | ITU-R P.525-5 — 计算自由空间损耗 | §2.4 FSPL 计算 |
| [10] | 工信部《民用无人驾驶航空器无线电管理暂行办法》(2024) | §3.3 频段合规，1430–1444 MHz 限制 |
| [11] | PX4 Safety (Failsafe) Configuration v1.16 | §3.4 遥控丢失与数据链丢失分别配置 |
| [12] | GStreamer `x264enc` 文档 | §4.3 zerolatency 的质量代价 |
| [13] | openwifi · `github.com/open-sdr/openwifi` | §2.3 可复用基础与许可证限制 |
| [14] | 本项目 `nebula_mvp` / `VERIFICATION.md` | §2.2 调度机制的软件验证结果 |
