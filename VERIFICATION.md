# MVP 验证记录

验证环境：Windows，Python 3.12.14，OpenCV 5.0.0.93，NumPy 2.3.5。以下为这台电脑上实际运行得到的结果，操作系统负载和视频内容会影响后续测量。

关于重复运行：基准测试的抖动和丢包由固定随机种子（默认 7）驱动。同一个种子重跑时，信道的丢包序列几乎相同（只按预热期发出的包数错开几个包），所以重复运行复现的是系统计时的波动，而不是信道的统计性质。下文“运行 N 次结论一致”除非注明，均为同一种子；突发丢包一节另用了种子 11 和 23 做独立重复。

## 自动检查

`python -m unittest discover -s tests -v`：**28 项通过**（2026-09-11，加入突发丢包模型后；地面选层改为等待高清后为 27 项，加入 FEC 后为 26 项，加入分层视频和修复计时后为 22 项，最初为 13 项）。

覆盖协议损坏与长度检查、控制与参数校验、视频分片/乱序/重复/超时、摄像头失败回退、最新遥控替换、优先级、有界队列与 TTL、整帧淘汰、虚拟发送时间线（醒晚后照常追赶、空闲链路从包就绪时刻开始、超过 20 ms 的卡顿计为丢失空口时间）、百分位数、真实 UDP 三业务收发、JPEG 重组与解码、HTTP 控制及配置、随机丢包、乱序遥控不回退、共享带宽上限、积压时 5 Mbps 链路实际速率不低于配置的 97% 且不超过配置，以及传播回调不会早于设定时延。

分层视频新增 8 项：视频字节预算与分片计费互逆、基础层在分片间抢占正在发送的高清帧、两层队列互不挤占、码率阶梯（无预算用最高档 / 按预算降档 / 无余量不升档 / 装不下时暂停 / 每帧最多升一档）、预算随实测吞吐修正且 FIFO 下不给预算、地面选层（不回退、同一时刻可升级；基础层等待同一帧高清、等到则不显示、等不到按时显示、等待时长随晚到时间自适应且有上下限、高清层暂停或普遍过晚时不等待）、跨层按帧计数（升级不重复计帧、每层只结算一次），以及真实 UDP 下 5 Mbps 显示高清层、降到 400 kbps 后 1.5 秒内高清层暂停并由基础层接管。

高清层 FEC 新增 4 项，另扩展 2 项：含冗余时的字节预算与实际打包计费一致且互逆；20 数据片 + 4 冗余片的帧丢掉任意 3 片（含最后一个短数据片）后在第 20 片到达时还原、此后迟到的片不会重新开帧，丢 5 片则超时丢弃，长度与布局不符的分片被拒收；冗余包数恰好是满足 99% 还原目标的最少值且上限为 100%；帧在缺片数不超过冗余片数时不记为丢失；丢包率估计在丢包开始时跟随短窗口快速上升、FIFO 下不提供；以及真实 UDP 下 5 Mbps、5% 丢包时实际发出冗余包、救回帧、高清占比 > 80%。另用 300 组随机丢片组合（1～23400 字节、1～8 片冗余）单独验证了编解码，全部还原。

突发丢包新增 1 项，另扩展 1 项：20 万个包的两状态信道在 5% 丢包、平均突发 8 时，测得丢包率在 5% ± 0.6 个百分点内、平均连丢长度在 8 ± 0.8 内；突发长度为 1 时与引入突发前的独立丢包逐包相同；改动丢包参数后信道从“好”状态重新开始、丢包率为 0 时不再丢包；突发长度超出 1～50 或非数值时拒绝配置。另用 40 万个包检查了 5% 丢包下突发 1/2/4/8/16，以及 20% 丢包突发 3、1% 丢包突发 10：丢包率和平均连丢长度都与设定值一致（突发 1 时为独立丢包的理论值 1.05）。

前端 `node --check nebula_mvp/static/app.js` 通过。

实际摄像头检查：`--source camera --headless --duration 2` 成功打开 **Camera 0**，生成 28 帧、地面端接收并校验 27 帧，没有回退警告。该检查未保存图像内容；统计见 `artifacts/camera-check/summary.json`。

## 拥塞实验

执行：

```powershell
.\.venv\Scripts\python.exe -m nebula_mvp.benchmark --verify --output artifacts/acceptance
```

每种模式依次使用相同的合成画面和随机种子。源为 640×360 JPEG，目标 15 FPS；遥测 10 Hz；遥控 50 Hz。共享容量先为 5 Mbps，3 秒后降到 500 kbps 并持续 10 秒；传播时延 20 ms，抖动 ±5 ms，丢包率 0%。表中是拥塞阶段最后 5 秒的样本。本节是计时修复前的记录：当时 500 kbps 链路实际约为配置的 95%（见最后一节），结论不受影响。

| 指标 | FIFO | Fusion |
|---|---:|---:|
| 遥控 P50 | 6308.642 ms | 33.332 ms |
| 遥控 P95 | 8197.127 ms | 43.554 ms |
| 遥控 P99 | 8223.513 ms | 45.529 ms |
| 遥测 P95 | 8069.669 ms | 50.116 ms |
| 完整视频帧率 | 2.4 FPS | 2.2 FPS |
| 视频平均传输延迟 | 6335.139 ms | 598.114 ms |

5 Mbps 预热阶段，两种模式接收约 14.4～14.5 FPS（包含启动时间），遥控 P99 分别为 79.730 ms 和 29.069 ms。界面持续运行时可达到 15 FPS。没有把已经拥塞的状态当作高带宽基线。

Fusion 在拥塞时丢弃等待中的旧帧，保留正在发送的帧，从而使遥控延迟保持在几十毫秒，同时持续交付完整视频帧。FIFO 在此场景积累了数秒队列。视频延迟截至完整帧到达，不包含浏览器渲染；结果不代表真实无线设备指标。

原始记录：

- `artifacts/acceptance/comparison.json` 与 `comparison.md`
- `artifacts/acceptance/naive/packets.csv` 与 `timeline.jsonl`
- `artifacts/acceptance/fusion/packets.csv` 与 `timeline.jsonl`

自动验收还检查了高带宽基线、控制样本数、Fusion P99 小于 250 ms、FIFO P99 大于 Fusion 三倍、Fusion 仍有完整视频帧，以及发送速率没有超过共享容量允许范围；全部通过。

## 分层视频实验（2026-09-10）

执行：

```powershell
.\.venv\Scripts\python.exe -m nebula_mvp.benchmark --verify --output artifacts/acceptance-layered
```

场景与上一节相同，增加第三组 `fusion-layered`：调度器同为 Fusion，视频改为 160×90 q40 基础层 + 按剩余容量选档的高清层。前两组仍为单层 640×360 q50，复现上一节的对比。统计拥塞阶段最后 5 秒；“骤降后冻结”是容量从 5 Mbps 降到 500 kbps 后 3 秒内画面两次更新之间的最长间隔。

| 指标 | FIFO | Fusion 单层 | Fusion 分层 |
|---|---:|---:|---:|
| 遥控 P50 | 6407.762 ms | 31.741 ms | 26.733 ms |
| 遥控 P95 | 8324.685 ms | 41.47 ms | 38.386 ms |
| 遥控 P99 | 8386.773 ms | 44.54 ms | 41.76 ms |
| 遥测 P95 | 8102.793 ms | 45.661 ms | 35.913 ms |
| 显示帧率 | 2.6 FPS | 2.4 FPS | **15.2 FPS** |
| 视频平均延迟 | 6436.208 ms | 562.3 ms | **60.059 ms** |
| 高清层占比 | 100% | 100% | 0%（高清层暂停） |
| 最长冻结 | 404 ms | 446 ms | **77 ms** |
| 骤降后 3 秒内最长冻结 | 402 ms | 447 ms | **119 ms** |
| 链路发送占用 | 100% | 100% | 62% |

500 kbps 下，扣除遥控、遥测和基础层后的剩余容量装不下高清层最低档（320×180 q20，约 3.8 KB），高清层暂停，画面由基础层维持。遥控 P99 没有因分层变差。链路占用只有 62%：剩余约 180 kbps 装不下一帧能及时送达的高清画面，按“画面只前进、不回退”的规则，迟到的高清帧不会被显示，所以不发。

5 Mbps 预热阶段，分层组约 14.9 FPS，高清层用最高档 640×360 q50，新显示画面中高清层占 97.8%（首帧先显示基础层），遥控 P99 27.126 ms。

自动验收在上一节检查项之外，另查分层组遥控 P99 < 250 ms、显示帧率 ≥ 10 FPS 且 ≥ 单层 3 倍、视频 P95 < 250 ms、最长冻结 < 500 ms、骤降后冻结 < 400 ms、5 Mbps 下高清层占比 ≥ 80%，以及每组模拟器保真度（节拍效率 ≥ 0.98、投递偏差 P95 < 5 ms）；全部通过。三组的节拍效率均为 1.0，投递偏差 P95 为 1.571～1.594 ms。本表为当前代码：协议 v2（每个视频分片头多 4 字节），地面选层已改为等待同一帧的高清，信道支持突发丢包（本场景无丢包）。FEC 不发任何冗余包；选层改动使骤降后冻结从 203 ms 降到约 120 ms（两次运行 117、119 ms），其余指标与此前的运行一致。

原始记录：`artifacts/acceptance-layered/` 下的 `comparison.json`、`comparison.md` 及三个子目录的 `packets.csv`、`timeline.jsonl`、`experiment.json`。

## 高清层 FEC 实验（2026-09-11）

执行：

```powershell
.\.venv\Scripts\python.exe -m nebula_mvp.benchmark --scenario loss --verify --output artifacts/acceptance-fec
```

三组都用 Fusion 调度，5 Mbps 无丢包预热 3 秒，然后保持 5 Mbps、开始 5% 独立随机丢包，运行 10 秒。依次为单层、分层无 FEC、分层 + FEC。“无法还原的高清帧”按切换后前 3 秒（丢包率估计还在追赶）和之后的 7 秒分开统计；其余指标取最后 5 秒。

| 指标 | 单层 | 分层，无 FEC | 分层 + FEC |
|---|---:|---:|---:|
| 遥控 P99 | 27.855 ms | 27.457 ms | 27.554 ms |
| 显示帧率 | 6.0 FPS | 13.6 FPS | **15.0 FPS** |
| 视频平均延迟 | 67.437 ms | 78.356 ms | 71.831 ms |
| 高清层占比 | 100% | 40% | **100%** |
| 最长冻结 | 398 ms | 199 ms | **82 ms** |
| 无法还原的高清帧，前 3 秒 | 75.6% | 64.4% | 4.4% |
| 无法还原的高清帧，之后 | 54.7% | 66.0% | **0.0%** |
| FEC 救回的帧 | 0 | 0 | 124 |
| 晚到高清（先显示了基础层） | 0 | 2 | 0 |
| 每帧冗余包 | 0 | 0 | 4.10 |
| 链路发送占用 | 59% | 64% | 77% |

本表为当前代码（地面选层已改为等待同一帧的高清，见下一节；信道默认独立丢包，与引入突发模型前逐包一致）。

- 不加保护时，20 片的高清帧在 5% 丢包下理论上 64.2% 无法还原，各次运行实测 55%～67%，一致。
- 加 FEC 后每帧配约 4 片冗余（约 20% 开销，链路占用从 64% 升到 77%），稳态下无法还原的高清帧各次运行为 0.0%～0.9%，与 99% 的设计目标一致；遥控 P99 不受影响。
- FEC 组高清占比各次运行为 97%～100%：没救回的高清帧由基础层顶上。偶尔有靠冗余片还原、比等待期稍晚完成的帧，记为“晚到高清”（每次运行 0～2 次）。
- 分层无 FEC 这次的最长冻结 199 ms：相邻两次采集都是两层全丢，画面停三个帧间隔。5 秒窗口里约四分之一的运行会碰到一次，属于信道本身的结果。
- 丢包刚开始时的过渡期：最初只用 2 秒窗口估计丢包率，前 3 秒有 11.1% 的高清帧没救回来（45 帧中 5 帧，全部在切换后 1.8 秒内），之后 7 秒 0 帧。改为同时用 0.5 秒和 2 秒窗口并取较大值后，各次运行的前 3 秒为 2.2%～4.4%（45 帧中 1～2 帧），稳态均为 0.9%。第一帧在还没观测到任何丢包时发出，无法避免。

自动验收另查三组遥控 P99 < 250 ms、无 FEC 时稳态无法还原的高清帧 ≥ 30% 但显示帧率 ≥ 13 FPS 且最长冻结 < 300 ms、FEC 组稳态 ≤ 2%、高清占比 ≥ 90%、显示帧率 ≥ 14 FPS、视频 P95 < 250 ms，以及每组模拟器保真度；全部通过。无 FEC 组的冻结门槛最初定为 200 ms，本次运行 199 ms 险些误报，按上一条的信道分析放宽到 300 ms（连续三次采集两层全丢约 2% 的运行会出现，停约 270 ms）。当前的显示逻辑在这台电脑上运行四次，结论一致。

原始记录：`artifacts/acceptance-fec/` 下的同名文件。

## 地面选层改为等待同一帧的高清（2026-09-11）

原规则：收到一帧“新鲜”的高清后 150 ms 内压住所有更新的基础层，防止画面在清晰和模糊之间来回闪。高清帧时断时续时（关闭 FEC 又有丢包），画面会停在旧的高清帧上，直到保持期结束。

新规则：基础层先等同一次采集的高清帧，等到就显示高清、基础层不显示；等不到就在等待期结束时显示基础层。等待时长跟随最近高清比基础层晚到的 P95 加 10 ms（20～150 ms）；最近 8 帧没有高清，或高清普遍晚于 150 ms 时不等待。

同一基准测试的修改前后（修改前的数据来自上一版代码的 `acceptance-fec` 与 `acceptance-layered`，已被覆盖；修改后为当前显示逻辑下各次运行的范围）：

| 指标 | 修改前 | 修改后 |
|---|---:|---:|
| 5% 丢包、分层无 FEC：显示帧率 | 8.2 FPS | **13.4～13.6 FPS** |
| 5% 丢包、分层无 FEC：最长冻结 | 240 ms | **150～199 ms** |
| 5% 丢包、分层 + FEC：最长冻结 | 134 ms | **82～96 ms** |
| 5 Mbps 骤降到 500 kbps：骤降后 3 秒内最长冻结 | 203 ms | **117～119 ms** |

分层无 FEC 的帧率已接近信道上限。以帧率 13.4 FPS 的那次运行为例：切换后的 151 次采集中没显示的 10 次，全部是高清层和基础层都被信道丢失（基础层 2 片，在 5% 丢包下本身约 9.75% 整帧丢失），没有一次是被显示规则丢掉的。最长冻结 199 ms 那次是相邻两次采集两层全丢，停了三个帧间隔；修改前的 240 ms 则是每当高清帧时断时续就会出现的保持期停顿。代价是替补显示的基础层要多等一个等待期：该组视频平均延迟从 40 ms 升到 78 ms（其中也包含统计口径的变化：延迟现在算到地面端把它设为当前画面，之前只算到重组完成）。

按容量扫描（一次性测量，脚本未入库；每档先跑 2 秒，再统计 5 秒）：

| 容量 | 丢包 | 高清档位 | 等待时长 | 晚到高清 / 显示帧数 | 帧率 | 视频 P95 |
|---:|---:|---|---:|---:|---:|---:|
| 0.5 Mbps | 0% | 暂停（不等待） | — | 0 / 75 | 15.0 | 65.5 ms |
| 1.0 Mbps | 0% | 320×180 q20 | 49 ms | 0 / 75 | 15.0 | 79.8 ms |
| 1.5 Mbps | 0% | 480×270 q18 | 56 ms | 0 / 75 | 15.0 | 82.1 ms |
| 2.5 Mbps | 0% | 640×360 q22 | 64 ms | 0 / 75 | 15.0 | 83.3 ms |
| 5 Mbps | 0% | 640×360 q50 | 53 ms | 0 / 75 | 15.0 | 71.5 ms |
| 1.5 Mbps | 5% | 320×180 q20 | 41 ms | 1 / 75 | 15.0 | 69.4 ms |
| 2.5 Mbps | 5% | 480×270 q30 | 50 ms | 0 / 75 | 15.0 | 75.8 ms |

无丢包时没有出现先糊后清的闪烁；高清层暂停时基础层不等待，视频延迟与修改前一致。

## 突发丢包下的 FEC（2026-09-11）

执行（种子 7 为默认值，11 和 23 用于独立重复）：

```powershell
.\.venv\Scripts\python.exe -m nebula_mvp.benchmark --scenario burst --verify --output artifacts/acceptance-burst
.\.venv\Scripts\python.exe -m nebula_mvp.benchmark --scenario burst --verify --seed 11 --output artifacts/acceptance-burst-seed11
.\.venv\Scripts\python.exe -m nebula_mvp.benchmark --scenario burst --verify --seed 23 --output artifacts/acceptance-burst-seed23
```

5 Mbps，平均丢包 5%，分层 + FEC；只改变两状态信道的平均突发长度（连续丢失的包数）。每组切换后运行 20 秒，除遥控 P99 取最后 5 秒外，其余统计切换 3 秒后的全部时间（约 255 帧）。解析预测对一帧 20 个数据片 + 4 个冗余片、在信道上连续排列的情况逐包做动态规划，得到无法还原的概率。

| 平均突发（设定） | 实测丢包 | 实测平均突发 | 无法还原的高清帧（种子 7 / 11 / 23） | 解析预测 | 显示的采集帧 | 高清占比 | 帧率 | 最长冻结 | 遥控 P99 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 4.7%～5.2% | 1.04～1.06 | 0.0% / 0.4% / 1.2% | 0.6% | 100% | 98%～99% | 15.0 | 86～90 ms | 27.2～27.9 ms |
| 2 | 4.5%～5.5% | 1.89～2.16 | 3.5% / 5.5% / 5.9% | 5.9% | 98.8%～99.6% | 94%～97% | 14.8～14.9 | 136～142 ms | 27.3～27.8 ms |
| 4 | 4.6%～5.2% | 3.79～3.95 | 9.0% / 7.8% / 8.2% | 9.8% | 98.4%～99.2% | 91%～93% | 14.8～14.9 | 127～147 ms | 27.0～27.6 ms |
| 8 | 5.3%～6.4% | 7.30～9.19 | 9.0% / 9.4% / 9.8% | 9.9% | 96.1%～97.7% | 93%～94% | 14.4～14.7 | 137～252 ms | 27.1～27.8 ms |
| 16 | 3.3%～4.8% | 10.68～15.32 | 7.8% / 6.2% / 4.3% | 8.4% | 97.7%～98.4% | 94%～96% | 14.7～14.8 | 132～156 ms | 26.9～28.5 ms |

每帧平均冗余包 3.2～4.7（冗余按实测平均丢包率配置，不随突发长度变化）。

- **FEC 只在独立丢包下达到设计目标。** 平均突发一到 2 个包，救不回的高清帧就升到约 6%，4～8 个包时约 8%～10%，都比平均丢包率还高。原因是冗余按独立丢包配成每帧约 4 片，任何一串超过 4 个包的连续丢失落在一帧上，这一帧就救不回来。
- **曲线在突发 4 以后走平，16 时略降。** 平均丢包率不变时，突发越长，突发的次数越少，被打中的帧也越少，这抵消了每次打中都救不回的效应。突发 16 时 20 秒里只有约 25 次突发，各次运行差别很大：种子 23 那次实测丢包率只有 3.3%，所以只丢了 4.3%。
- **实测与解析预测一致**（突发 1～8 在预测值附近；突发 16 的实测丢包率偏低，结果也偏低），说明模拟器里其他业务包的穿插对结论影响不大。
- **更正：** 最初只用种子 7 时，突发 2 两次测到 1.9% 和 3.5%，我曾据此认为短突发下 FEC 还扛得住，并把与预测的差距归因于其他包的穿插。换用种子 11、23 后是 5.5% 和 5.9%，与预测一致。种子 7 那两次共用几乎相同的丢包序列，是同一个信道样本偶然偏轻，不是两次独立的证据。
- **画面由基础层维持：** 各档都有 ≥96% 的采集帧被显示，帧率 ≥14.3；最长冻结一般在 150 ms 以内；表中最大为种子 7 突发 8 的 252 ms，另一次未入库的同种子运行达到 322 ms（一串突发把相邻几帧的两层都打掉了）。遥控 P99 在所有档位都约 27 ms，不受影响。

只靠加冗余能扛住多少突发（解析计算，一帧 20 个数据片、平均丢包 5%、包在信道上连续排列）：

| 平均突发 | 保 99% 还原需要的冗余片 | 冗余开销 | 保 95% 还原需要的冗余片 |
|---:|---:|---:|---:|
| 1 | 4 | 20% | 3 |
| 2 | 8 | 40% | 5 |
| 4 | 14 | 70% | 7 |
| 8 | 23 | 115% | 10 |
| 16 | 39 | 195% | 13 |

结论：在 5% 平均丢包下，同一帧内的纠删码只对长度不超过约 1～2 个包的丢失有效；对付更长的突发，单靠加冗余开销太大。上表是**帧间交织之前**的基线。之后的实现改为：测到平均连丢 ≥1.5 时把相邻 2～3 帧高清分片交错上链路，并按突发长度略增冗余；独立丢包（突发 1）路径保持上表行为。新数字以重跑 `benchmark --scenario burst --verify` 为准。

原始记录：`artifacts/acceptance-burst/`、`acceptance-burst-seed11/`、`acceptance-burst-seed23/`。三次运行的验收全部通过：实测丢包率和平均突发长度都在按“几何分布连丢次数之和”估算的 3 倍标准差以内，各组遥控 P99 < 250 ms，突发 1 时无法还原的高清帧 ≤ 2%，模拟器保真度正常。

## 模拟器计时修复（2026-09-10）

原实现每发一个包就 `asyncio.sleep(串行时间)`，醒来后再从当前时刻发下一个包。这台电脑上每次约多睡 1 ms，这段时间就成了链路空闲，而且包越小占比越大。现改为虚拟发送时间线：每包从“链路空闲且包就绪”的时刻开始，发送循环睡到虚拟完成时刻，醒晚了也从虚拟完成时刻接着发，误差不再累积。

链路积压时的实际速率（一次性测量，脚本未入库：在 FIFO 模式下注入约 3.5 秒的 DATA 积压，统计 2 秒内的计费字节；“修复前”一列用改动前的代码备份，方法相同）：

| 配置容量 | 修复前 | 修复后 |
|---:|---:|---:|
| 0.5 Mbps | 0.477 Mbps（95.3%） | 0.500 Mbps（99.9%） |
| 1.5 Mbps | 1.365 Mbps（91.0%） | 1.499 Mbps（100.0%） |
| 5 Mbps | 3.307 Mbps（66.1%） | 4.997 Mbps（99.9%） |
| 10 Mbps | 5.876 Mbps（58.8%） | 10.000 Mbps（100.0%） |

5 Mbps 这一档由自动测试 `test_backlogged_link_sends_at_configured_rate` 持续覆盖：修复前该测试失败（1.5 秒内只发出应有字节的 66%），修复后通过。修复后四档的节拍效率均为 1.0，投递偏差 P50 约 0.5 ms、P95 约 1.3～1.4 ms，不随速率变化。

对已有结论的影响：

- 500 kbps 的 FIFO / Fusion 对比：修复前链路约为 95%，结论不变；修复后单层 Fusion 为 2.2 FPS / 约 555 ms（修复前约 595 ms）。
- 分层视频在同一标称容量下的高清档位整体上移（修复前 → 修复后）：1.5 Mbps 从 320×180 q30 到 480×270 q18，2.5 Mbps 从 480×270 q30 到 640×360 q22，5 Mbps 从 640×360 q35 到 q50。修复前高清层预算靠实测吞吐修正避开了这一偏差，所以当时也没有卡顿，只是档位偏低。

2026-09-10 另发现一次异常运行 `artifacts/acceptance-final/`：其中 Fusion 链路占用只有 46%，只得到 0.4 FPS / 1439 ms；用同一份代码复跑两次，均为 93% 占用、2.2 FPS / 约 595 ms，与第一节一致。推测是当时主机负载过重。这份记录不计入结论。现在基准测试会检查模拟器保真度，这类运行会被直接判为无效。
