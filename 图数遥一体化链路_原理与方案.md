# 图数遥三合一无线链路系统 —— 工作原理、技术需求与初步实现方案

> 分析对象：Nebula-7 类无人系统数字通信链路（1 中心 + 16 终端，星形自组网，标称 12 km / <15 ms）
> 说明：本文基于公开宣传页的指标做逆向推演，用于理解原理与技术方案设计，不代表厂商实际内部实现。

---

## 一、系统组成与角色划分

整套链路是**一对多的星形无线专网**，只有两种角色：

| 角色 | 部署位置 | 职责 |
|---|---|---|
| 中心节点（Hub / Master） | 地面端，接手持地面站、笔电、遥控器 | 发射同步信号、统一分配时隙与带宽、维护网络成员表 |
| 终端节点（Remote / Slave） | 机载或车载，接飞控、云台 IPC、机器人控制器 | 同步入网、按分配时隙收发、对上层做协议透传 |

对上层设备而言，两端模块合起来相当于**一根"看不见的网线 + 几根看不见的串口线"**——飞控只知道自己在跟地面站说 MAVLink，并不知道中间隔着 12 km 无线。这是整个产品设计的核心思想：**链路对业务透明**。

---

## 二、工作原理

### 2.1 "图数遥三合一"：一条链路承载三种业务

三类业务的物理性质完全不同，这是设计难点所在：

| 业务 | 方向 | 带宽 | 时延要求 | 可靠性要求 | 数据特征 |
|---|---|---|---|---|---|
| 图传（视频回传） | 下行（机→地）为主 | 2–20 Mbps | 中（100 ms 级可接受） | 允许丢帧 | 突发、大包、速率可变 |
| 数传（状态同步） | 双向 | 10–500 kbps | 中 | 需重传保证 | 周期小包 + 突发 |
| 遥控（控制指令） | 上行（地→机） | < 10 kbps | **极低且抖动要小** | **极高，不能丢** | 严格周期（如 SBUS 14 ms） |

**复用方式：TDD 帧结构 + 时隙划分 + QoS 优先级调度。**

链路把时间切成固定长度的空口帧（典型 2–10 ms），每帧再切成若干时隙：

```
|<----------------- 一个 TDD 帧（如 5 ms） ----------------->|
| 同步/信标 | 遥控上行 | 数传双向 | 图传下行（剩余全部时隙）  | 保护间隔 |
|  固定     |  固定    |  半固定  |  动态分配                 |          |
```

- **遥控时隙固定预留**：无论图传多忙，遥控的时隙永远在，且位置固定 → 时延确定、抖动小。这是"控制指令必达"的根本保证。
- **图传占用剩余带宽，尽力而为**：链路变差时先降视频码率，而不是丢遥控。
- **数传插空并带 ARQ 重传**：小包、可重传，对时延不敏感。

三者在协议栈上表现为三个优先级队列，调度器按 `遥控 > 数传 > 图传` 严格优先级出队。

### 2.2 物理层：数据是怎么飞出去的

一条典型的发送链路：

```
业务数据 → CRC 校验 → 信道编码(LDPC/Turbo) → 交织 → 星座映射(QPSK/16QAM/64QAM)
        → OFDM 子载波映射 → IFFT → 加循环前缀CP → 上变频 → 功放 → 天线
```

关键技术点：

- **OFDM 多载波调制**：把宽带信号拆成许多窄带子载波，每个子载波经历的信道近似平坦，配合循环前缀可以彻底消除多径造成的符号间干扰。无人机低空飞行、地面反射多径严重，这是必选项。
- **自适应调制编码（AMC）**：接收端实时上报 RSSI / SNR / 误包率，发射端据此切换 MCS。近距离用 64QAM + 高码率跑满带宽；飞到 10 km 外自动降到 QPSK + 1/2 码率，牺牲速率保住连接不断。**这是"远距离"和"高码率"能兼得的原因——它们不是同时成立的。**
- **TDD 双工**：收发同频、分时进行。好处是上下行比例可按需配置（图传业务上下行严重不对称，下行可以配到 80% 以上），且不需要双工器，成本体积都低。
- **双天线 MIMO / 分集**：模块上的两根天线，近距离可做空间复用翻倍速率，远距离退化为接收分集提升灵敏度。
- **抗干扰**：跳频、自动选频（上电扫描全频段选最干净的信道）、窄带干扰陷波。

### 2.3 链路预算：12 km 是怎么来的

自由空间路径损耗：

```
FSPL(dB) = 32.44 + 20·lg(f/MHz) + 20·lg(d/km)
```

以 2.4 GHz、12 km 计算：`32.44 + 67.60 + 21.58 ≈ 121.6 dB`

链路预算：

```
接收功率 = 发射功率 + 发射天线增益 + 接收天线增益 − 路径损耗 − 馈线/极化损耗
         = 30 dBm(1W) + 5 dBi + 8 dBi − 121.6 dB − 3 dB
         ≈ −81.6 dBm
```

接收灵敏度（5 MHz 带宽、QPSK 1/2）：

```
热噪声底 = −174 + 10·lg(5×10⁶) = −107 dBm
+ 噪声系数 5 dB → −102 dBm
+ 解调所需 SNR 3 dB → 灵敏度 ≈ −99 dBm
```

**链路余量 ≈ 17 dB** —— 数字上完全成立。这解释了几件事：

1. 12 km 是**窄带低阶调制**下的距离，此时视频码率必然很低。宣传页把"12 km"和高码率图传分开写，不是巧合。
2. 频率越低传得越远。同样条件下 1.4 GHz 的路损比 2.4 GHz 少约 4.7 dB，5.8 GHz 则多约 7.7 dB。行业设备偏好 1.4 GHz 正是为此。

**还必须满足视距和菲涅尔区净空**（这是"开阔无遮挡"的量化含义）：

```
第一菲涅尔区半径 r₁ = 8.66·√(d/f) = 8.66·√(12/2.4) ≈ 19.4 m
地球曲率隆起（中点，k=4/3） ≈ 2.1 m
→ 收发连线中点需离开障碍物 ≈ 21.5 m 以上
```

结论：**地面对地面 12 km 需要架高塔；而无人机飞到 100–300 m 高度天然满足**。这就是为什么这类链路的标称距离总是在空对地场景才能复现。

### 2.4 时延分解：<15 ms 到底是哪一段

宣传页脚注已经承认了：*"不代表摄像头采集至显示端的完整视频链路时延"*。完整端到端时延应该是：

| 环节 | 典型耗时 |
|---|---|
| 摄像头曝光 + 出图 | 1 帧 ≈ 33 ms @30fps（可用高帧率降低） |
| H.265 编码 | 5–30 ms |
| 打包 + 发送缓冲 | 2–10 ms |
| **空口传输（TDD 帧对齐 + 调度 + 空中飞行）** | **5–15 ms ← 厂商标称的就是这一段** |
| 接收抖动缓冲（jitter buffer） | 10–50 ms |
| 解码 + 渲染显示 | 10–30 ms |
| **合计（真实体感）** | **80–160 ms** |

理解要点：**15 ms 是"模块入口到模块出口"的链路时延，是模组厂商唯一能负责的部分**；真正影响操控手感的大头在编解码和抖动缓冲。做系统集成时，优化重点往往不在链路而在编码器配置和 jitter buffer 策略。

遥控通道要单独看：它不经过编解码，`地面 SBUS 输入 → 空口 → 机载 SBUS 输出` 的时延就是链路时延本身，通常 < 20 ms，这才是"低时延"真正有意义的地方。

### 2.5 星形自组网与调度

**入网流程（终端上电后自动完成）：**

```
扫频搜索信标 → 频率/时间同步 → 发起入网请求（携带 ID/能力）
→ 中心节点鉴权、分配短地址与时隙 → 进入业务态 → 周期心跳维持
```

**为什么是星形而不是 Mesh：**

| | 星形（本方案） | Mesh |
|---|---|---|
| 调度 | 中心统一分配，时隙确定 | 分布式竞争/路由 |
| 时延 | 确定、可预测 | 随跳数和路由收敛波动 |
| 覆盖 | 受限于中心节点覆盖范围 | 可多跳延伸 |
| 可靠性 | 中心失效则全网瘫痪 | 无单点故障 |
| 适用 | 集群作业、编队，要求控制时延确定 | 大范围、无固定基础设施 |

对"控制类"业务，**时延确定性远比覆盖延展性重要**，所以选星形。

**16 个终端的带宽约束（重要）：**
"1 带 16" 是拓扑容量，不是并发满码率能力。若总物理层速率约 40 Mbps，16 节点平均每台仅约 2.5 Mbps ——够 720p H.265，不够 1080p 高码率。实际使用必然是**按需动态分配**：只有被"点名"的那一路视频给高码率，其余只保留数传和遥控。这是方案设计时必须写进需求的约束。

### 2.6 接口层：怎么接进现有系统

模块本质是一个**跨越无线的透明网桥 + 多路串口隧道**：

| 接口 | 对接设备 | 承载协议 | 实现方式 |
|---|---|---|---|
| ETH1 / ETH2 | 云台 IPC、机器人控制器、地面站 PC | RTSP/RTP 视频流、ROS2/DDS、TCP/UDP | 二层网桥，两端同网段，MAC 地址透传 |
| UART1 / UART2 | 飞控、载荷 | MAVLink、Modbus、自定义 | 串口数据封装成帧，透明隧道 |
| SBUS | 遥控接收机 / 舵面 | SBUS（100 kbps、8E2、反相、25 字节、周期 14 ms） | 解析 16 通道值后单独走高优先级时隙 |
| Type-C (NIC) | 地面端笔电 | RNDIS/CDC-ECM USB 网卡 | 免驱虚拟网卡 |
| Type-C (UPGRADE) | 上位机 | 固件升级、参数配置 | — |

注意 SBUS 的处理方式：它不是简单透传字节流，而是**解析出通道值再重新组包**。这样 14 ms 一帧的数据只需几十字节，可以塞进最小的固定时隙，且丢一帧可以用上一帧的值填充，操控不会跳变。

---

## 三、技术需求整理

### 3.1 功能需求

| 编号 | 需求 | 说明 |
|---|---|---|
| F-01 | 单链路并发承载图传/数传/遥控三类业务 | 三者互不阻塞，遥控具有最高优先级 |
| F-02 | 星形组网，1 中心 + ≥16 终端 | 终端上电自动入网，无需人工配置 |
| F-03 | 以太网二层透明桥接 | 两端设备同网段可直接互访 |
| F-04 | 双路 UART 透传 | 波特率 9600–921600 可配 |
| F-05 | SBUS 通道解析与还原 | 16 通道，输出周期稳定 |
| F-06 | 自动选频与抗干扰 | 上电扫频选优，运行中可切换 |
| F-07 | 带宽动态分配 | 支持指定某终端为主视频源 |
| F-08 | 链路状态可视化 | RSSI、SNR、丢包率、当前 MCS、实时码率 |
| F-09 | 断链自动重连 | 失联后持续重搜，恢复时间 < 3 s |
| F-10 | 加密与鉴权 | AES-128/256 空口加密，白名单入网 |
| F-11 | 固件在线升级 | 支持中心节点远程升级终端 |

### 3.2 性能需求

| 指标 | 目标值 | 测试条件 |
|---|---|---|
| 视距通信距离 | ≥ 12 km | 空对地，机载高度 ≥ 150 m，无遮挡，最低阶 MCS |
| 空口单向时延 | ≤ 15 ms | 模块入口到出口，满业务负载 |
| 遥控端到端时延 | ≤ 25 ms | SBUS 输入到 SBUS 输出 |
| 遥控时延抖动 | ≤ ±3 ms | — |
| 链路总吞吐 | ≥ 20 Mbps @ 2 km；≥ 2 Mbps @ 12 km | 视距 |
| 单终端保底带宽 | ≥ 512 kbps | 16 节点满载时 |
| 遥控丢包率 | ≤ 10⁻⁵（含重传） | — |
| 入网时间 | ≤ 5 s | 冷启动 |
| 频段 | 1.4 GHz / 2.4 GHz / 5.8 GHz 可选 | 依当地法规 |
| 发射功率 | ≤ 30 dBm，可调 | 满足 EIRP 法规限值 |
| 功耗 | ≤ 8 W（终端） | 典型业务 |
| 供电 | 9–36 V DC 宽压 | 兼容 3S–8S 电池 |
| 重量 | 终端 ≤ 150 g | 机载载重约束 |
| 工作温度 | −20 ℃ ~ +60 ℃ | 无风冷条件下自然散热 |
| 防护等级 | IP54 及以上 | — |

### 3.3 约束与合规需求

- **频谱合规**：中国境内 2.4 GHz / 5.8 GHz ISM 需符合工信部对功率和占用带宽的规定；1.4 GHz 属受管制频段，需取得无线电台执照。方案必须支持按区域配置频率表和功率上限。
- **信息安全**：控制指令加密与防重放（时间戳 + 序列号），防止链路劫持。
- **失控保护**：链路中断后，机载端 SBUS 输出需按预设策略动作（保持/返航信号/失效保护值），不能输出乱码。

---

## 四、初步实现方案

### 4.1 技术路线选型

| 路线 | 描述 | 优点 | 缺点 | 评价 |
|---|---|---|---|---|
| A. 私有图传 SoC | 采用海思/展锐等专用图传基带芯片 | 集成度高、功耗低、量产成本低 | 协议栈黑盒，定制自由度低，供应受限 | 适合量产，不适合学习/研究 |
| B. **SDR 自研（推荐）** | AD9361/AD9364 射频收发 + Zynq/ZU+ FPGA 基带 + ARM 协议栈 | 物理层完全可控，可自定义 TDMA 帧结构，便于研究与迭代 | 开发量大，功耗体积偏高，成本高 | **原理理解与原型验证首选** |
| C. LTE/5G 私网模组 | 采用私网基站 + CPE（如 1.4 GHz 宽带专网） | 协议成熟，多用户调度现成 | 时延偏大（20–50 ms），成本高，架构重 | 适合大范围指挥，不适合低时延遥控 |
| D. Wi-Fi 改造 | 802.11 芯片改 TDMA 固件 | 成本极低 | CSMA 本质不适合确定性调度，改造深度受限 | 仅适合低成本入门方案 |

**建议：原型阶段走 B（SDR），验证帧结构、调度算法和 AMC 策略；产品化阶段再评估是否切到 A 降本。**

### 4.2 硬件架构框图

```
┌──────────────────────────────────────────────────────────┐
│                      终端 / 中心节点模块                   │
│                                                          │
│  [天线1]──┐                                              │
│           ├─[射频前端: PA/LNA/收发开关/滤波器]            │
│  [天线2]──┘            │                                 │
│                        ▼                                 │
│              [AD9361 宽带收发器 2T2R]                     │
│                        │ LVDS                            │
│                        ▼                                 │
│   ┌────────────────────────────────────────┐             │
│   │  Zynq UltraScale+ MPSoC                │             │
│   │  ┌──────────────┐  ┌────────────────┐  │             │
│   │  │ PL (FPGA)    │  │ PS (ARM A53)   │  │             │
│   │  │ OFDM 调制解调 │◄─┤ MAC 调度器      │  │             │
│   │  │ LDPC 编解码  │  │ 组网/入网管理    │  │             │
│   │  │ 同步/信道估计 │  │ QoS 队列        │  │             │
│   │  │ AGC/定时环   │  │ 网桥/串口隧道    │  │             │
│   │  └──────────────┘  └────────────────┘  │             │
│   └────────────────────────────────────────┘             │
│         │         │         │        │                   │
│      [GMAC]    [UART×2]  [SBUS]   [USB]                  │
│         │         │         │        │                   │
│   [ETH1/ETH2]  [飞控]   [接收机]  [Type-C]                │
│                                                          │
│  [DC-DC 9~36V] [温度监测] [铝合金散热外壳]                │
└──────────────────────────────────────────────────────────┘
```

要点：
- 视频**不在模块内编码**——IPC 已输出 H.265 码流，模块只做透传。这是"低时延"和"低功耗"的关键设计决策。
- 外壳即散热器（宣传图里模块的密集散热齿正是为此），FPGA 方案功耗高，热设计是硬约束。

### 4.3 软件分层

```
┌─────────────────────────────────────────┐
│ 应用层  配置管理 / 状态上报 / OTA 升级     │
├─────────────────────────────────────────┤
│ 适配层  以太网桥 | 串口隧道 | SBUS 编解码  │
├─────────────────────────────────────────┤
│ 网络层  节点管理 / 路由(星形直连) / 加密   │
├─────────────────────────────────────────┤
│ MAC 层  TDMA 帧调度 | QoS 队列 | ARQ 重传 │
│         带宽动态分配 | AMC 决策           │
├─────────────────────────────────────────┤
│ 物理层  OFDM 收发 / 编译码 / 同步 (FPGA)  │
└─────────────────────────────────────────┘
```

### 4.4 关键算法设计

**① TDMA 帧结构（示例，5 ms 帧）**

| 时隙 | 长度 | 用途 |
|---|---|---|
| BCH | 0.2 ms | 中心节点广播信标：帧号、时隙分配表、系统参数 |
| UL-CTRL | 0.3 ms | 遥控上行，固定预留 |
| UL-DATA | 0.5 ms | 终端数传上行 + 入网请求随机接入 |
| DL-DATA | 0.5 ms | 数传下行 + 调度指令 |
| DL-VIDEO | 3.3 ms | 图传下行，动态分配给各终端 |
| GP | 0.2 ms | 保护间隔（覆盖 12 km 传播时延约 40 μs + 收发切换） |

**② AMC 决策**：滑动窗统计 SNR 与 BLER，双门限迟滞切换 MCS，防止边界抖动。

```
if BLER > 10% for 3 frames:      降一级 MCS
if SNR > threshold[k+1] + 2dB and BLER < 1% for 20 frames:  升一级 MCS
```

**③ 带宽动态分配**：中心节点每 100 ms 重算一次时隙表。保底带宽先分配给所有在网终端，剩余带宽按"当前主视频源 > 有数据待发 > 空闲"加权分配。

**④ 失控保护**：终端连续 5 帧未收到信标即判定失联，SBUS 输出切换到预设 failsafe 值并置位失效标志给飞控。

### 4.5 开发阶段规划

| 阶段 | 内容 | 里程碑 |
|---|---|---|
| P1 链路预算与选型（2 周） | 频段确定、器件选型、链路预算表、法规调研 | 输出方案论证报告 |
| P2 物理层仿真（4 周） | MATLAB/GNU Radio 搭建 OFDM 收发链路，加多径信道验证 BER 曲线 | 仿真达标 |
| P3 单点对传（6 周） | FPGA 实现物理层，实验室有线对接跑通单向数据 | 桌面短距通信成功 |
| P4 MAC 与组网（6 周） | TDMA 调度、入网流程、QoS 队列、AMC | 1 中心 + 3 终端稳定组网 |
| P5 接口适配（3 周） | 网桥、串口隧道、SBUS，接真实飞控与 IPC | 联调通过 |
| P6 外场测试（4 周） | 距离、时延、抗干扰、多机并发实测 | 指标达标报告 |

### 4.6 测试验证方法

| 指标 | 方法 |
|---|---|
| 通信距离 | 外场空对地飞行，逐点记录 RSSI/BLER/吞吐随距离曲线；先在实验室用可变衰减器模拟等效路损做标定 |
| 空口时延 | 两端接同一台示波器，在入口和出口打触发脉冲，直接量时间差（不要用软件打时间戳，会引入系统误差） |
| 时延抖动 | 连续采集 10000 个遥控帧的到达间隔，统计标准差与 P99 |
| 吞吐 | iperf3 跑 UDP，逐级加压找拐点 |
| 多机并发 | 16 台终端同时入网，观察各自保底带宽是否满足、入网是否互相干扰 |
| 抗干扰 | 信号源注入同频/邻频干扰，记录链路降级和恢复行为 |

### 4.7 主要风险

| 风险 | 影响 | 应对 |
|---|---|---|
| 12 km 与高吞吐不可兼得 | 需求冲突 | 需求中明确分档：距离指标与吞吐指标分开定义，写清各自的测试条件 |
| FPGA 功耗与散热 | 高温降频、机载重量超标 | 早期做热仿真；评估低功耗基带替代方案 |
| 频谱合规 | 无法合法使用 | 提前确认频段许可，软件支持区域参数表 |
| 16 节点带宽不足 | 用户体感与宣传不符 | 明确"1 主视频 + N 路数传遥控"的典型工作模式 |
| 多径与地面反射 | 低空段链路劣化 | 加强信道估计与均衡，采用天线分集 |

---

## 五、给汇报的一句话总结

这套系统的本质是：**用 TDD 帧结构和严格优先级调度，把三类性质迥异的业务塞进同一条 OFDM 无线链路**——用固定时隙保住遥控的时延确定性，用自适应调制编码换取距离与速率的动态平衡，用中心节点集中调度实现 16 台设备的确定性组网，再用透明网桥和串口隧道让上层设备感知不到无线的存在。
